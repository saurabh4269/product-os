"use client";

import { LabsHub } from "@/components/ref/labs-hub";

/** Labs hub — architecture diagrams + eval fixtures. */
export default function LabsPage() {
  return (
    <div className="page-pad fade-in">
      <LabsHub />
    </div>
  );
}
